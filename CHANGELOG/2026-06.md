# Changelog — 2026-06

### 2026-06-22 — chore(box/fedora): re-stamp to schema 2026.173.2300 + migrate http/addr checks + add plugin: interface check

The observe verbs http/interface/addr left the closed `#Op` into dedicated builtin plugin units
(charly superproject, observe-verb wave 2). `charly migrate` re-stamps this repo's `charly.yml` to
the new schema HEAD and converts its `http:`/`addr:` CHECK steps to `plugin: <verb>` + `plugin_input:`.
Also adds a `check-interface-plugin` step (`plugin: interface`) to `check-pod` so the corpus-unused
`interface` verb's new dispatch is exercised live (it finds the loopback). Gate: `charly box validate`
exit 0 + check-pod.

### 2026-06-22 — chore(box/fedora): re-stamp to schema 2026.173.1742 + migrate goss-verb checks + add plugin: dns check

The observe-only goss verbs (port/process/dns) left the closed `#Op` into dedicated builtin
plugin units (charly superproject). `charly migrate` re-stamps this repo's `charly.yml` to the
new schema HEAD and converts its `port:`/`process:`/`dns:` CHECK steps to `plugin: <verb>` +
`plugin_input:` (published `port:` + install `package:`/`service:` untouched, isStepNode-gated).
Also adds a `check-dns-plugin` step (`plugin: dns`) to `check-pod` so the corpus-unused `dns`
verb's new dispatch is exercised live. Gate: `charly box validate` exit 0 + check-pod.

### 2026-06-22 — test(box/fedora): exercise the CheckVerbProvider-plugin path in check-pod

Adds a `check-runverb-plugin` step (`plugin: examplerunverb`) to the `check-pod` disposable
bed, exercising charly's new `runPluginVerb` dispatch that lets a `CheckVerbProvider` plugin
unit keep the live `*Runner` (the executor enabler for the execution-needing goss verbs).
Lands in lockstep with the superproject's runPluginVerb-enabler cutover.

### 2026-06-22 — chore(box/fedora): re-stamp to schema 2026.173.1616 (matching-to-plugin cutover)

The `matching` check verb left the closed `#Op` schema (the charly superproject added a dedicated
matching plugin unit + the `matching-to-plugin` MigrationStep raising `LatestSchemaVersion`
2026.172.0006 → 2026.173.1616). `charly migrate` re-stamps this repo's `charly.yml` `version:` to
the new schema HEAD; no content change here (matching was already migrated / unused). Gate:
`charly box validate` exit 0 (git-workflow B7 schema-HEAD re-stamp).

### 2026-06-22 — test(box/fedora): exercise the new matching plugin verb in check-pod

Adds a `check-matching-plugin` step to the `check-pod` disposable bed so the R10 gate exercises
charly's newly-extracted `matching` built-in plugin verb (the superproject's Phase-1 verb
extraction) live: `plugin: matching` + `plugin_input: {matching: "opencharly", contains:
{contains: "charly"}}`. Lands in lockstep with the superproject's matching-extraction cutover.

### 2026-06-21 — chore(box/fedora): migrate to schema 2026.172.0006 (EDGE-INHERIT cutover D — `box:` merged into `candy:`)

`charly migrate` applies the superproject's EDGE-INHERIT cutover D (the `box:` KIND merges INTO `candy:` —
`base:`/`from:` presence makes a candy a full IMAGE) to this submodule's per-box dirs: every discovered
`box/<name>/charly.yml` image renames its `box:` KIND discriminator to `candy:` (keeping its `base:`/`from:`
marker, which is what now makes it an image), and the schema HEAD is re-stamped to `2026.172.0006`. No
behavior change — the image is identical, only the kind-at-edge spelling moves. (R10 ran the check-pod bed — PASS.)

### 2026-06-21 — chore(box/fedora): migrate to schema 2026.172.0004 (EDGE-INHERIT cutover B — `bundle:` eliminated)

`charly migrate` applies the superproject's EDGE-INHERIT cutover B (the `bundle:` deploy KIND is eliminated —
substrate kinds at the EDGE) to this submodule's `charly.yml`: every disposable check bed / deploy moves from
`bundle:{box:X}` to `pod:{image:X}` (and `bundle:{vm:V}`→`vm:{from:V}`, a targetless `bundle:`→`group:`),
and the schema HEAD is re-stamped to `2026.172.0004`. No behavior change — the deploy shape is identical, only
the kind-at-edge spelling moves. (R10 for the cutover ran check-pod / check-sway-browser-vnc-pod / check-jupyter-pod beds — PASS.)

### 2026-06-21 — chore(box/fedora): migrate to schema 2026.172.0002 (EDGE-INHERIT cutover A probe-verb rename)

`charly migrate` re-stamps the submodule to the new schema HEAD `2026.172.0002` for the superproject's
EDGE-INHERIT cutover A (the step probe verbs `k8s`/`group` → `kube`/`unix_group`). This submodule declares
no colliding step verb, so the only change is the version re-stamp; no behavior change. (R10 for the
cutover ran `check-pod` from this submodule — build → check → deploy → check-live → fresh update — PASS.)

### 2026-06-21 — test(check-pod): compose the out-of-tree plugin candy (F1 unified-schema R10)

check-pod's candy list gains `@github.com/overthinkos/overthink/candy/plugin-example-external` — the
reference OUT-OF-TREE plugin (its own Go module serving over go-plugin gRPC via the charly SDK). Its
baked `externalprobe-out-of-proc` check now rides into the bed alongside the built-in `exampleprobe-baked`
check, so check-pod is the R10 bed for main's unified plugin-schema cutover: it proves a built-in (in-proc)
AND an out-of-tree (out-of-process) plugin BOTH dispatch through the provider registry with their
`plugin_input` validated against the plugin's own CUE schema served over the Describe channel — zero
builtin/external distinction. R10: `charly check run check-pod` PASS (steps=10); `externalprobe-out-of-proc`
passes with the marker round-tripping author → wire → external process → result.

---

### 2026-06-21 — test(check-pod): bake the exampleprobe plugin-verb check (plugin-foundation R10)

check-pod's box plan gains a runtime `plugin: exampleprobe` check — the R10 bed proof that
main's unified provider registry + the generic `plugin:` verb dispatch + the `plugin_input`
round-trip work end-to-end on a real disposable pod. Baked into the description label (a box
plan step, NOT a bundle-overlay step — those do not merge into `charly check live`, a separate
finding), so check-live actually runs it (`[exampleprobe-baked] check-pod-plugin-ok`).

---

### 2026-06-19 — feat: adopt v2026.170.1105 candy pins (`v2026.170.1539`)

`charly box reconcile --remote` bumped all 151 `@github.com/overthinkos/overthink/candy/*` pins from
v2026.166.1222 → v2026.170.1105 (the unified-node-model candies). Standalone `charly box validate`
(no `CHARLY_REPO_OVERRIDE`) passes against the real remote tag. Closes the §3 cutover's deferred candy-pin lag.

---

### 2026-06-19 — feat!: migrate to the unified name-first node model + R5 stale-ref sweep (`v2026.170.1105`)

Every `charly.yml` entity migrates to the unified `<name>: {<kind>: …, <children>}`
node form: `deploy:`→`bundle:`, venue + membership from TREE POSITION (authored
`on:`/`pod:`/`peer:`/`nested:`/`target:` removed), `${HOST:<member>}` the sole
cross-member address, `check:` beds → disposable bundles. R5 sweep removes every
reference to the deleted identifiers — `charly deploy`→`charly bundle`,
`DeploymentNode`→`BundleNode`, `${PEER_ENDPOINT}`/`${PEER_HOST}`→`${HOST:…}`.
Validated via the local-main override; the standalone candy `@github` pins bump
in the immediate-next `charly box reconcile` cutover. Full unified-node-bundle
change: superproject `CHANGELOG/`.

---

### 2026-06-17 — chore(changelog): adopt the per-repo monthly `CHANGELOG/` structure

This repository now keeps its own `CHANGELOG/` directory with one file per
calendar month (`YYYY-MM.md`), matching the project-wide history model in which
the superproject and every submodule keeps its OWN `CHANGELOG/`. History is
repo-scoped, never centralized in a single root file. The repo's signpost
(`CLAUDE.md` / `README.md`) was updated to point here. See the superproject
`CLAUDE.md` "Where things are documented" and `/charly-internals:skills`.
