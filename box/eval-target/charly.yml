box:
    # Throwaway benchmark target used by `charly eval run eval-target`.
    # The eval-target layer ships a deliberately-failing BDD scenario
    # that an AI solves by adding a write: task. Fedora base + minimal
    # layer stack keeps per-iteration rebuild fast.
    name: eval-target
    base: fedora
    candy:
        - '@github.com/overthinkos/overthink/candy/claude-code:v2026.162.0847'
        - '@github.com/overthinkos/overthink/candy/charly-mcp:v2026.162.0847'
        - '@github.com/overthinkos/overthink/candy/eval-target-layer:v2026.162.0847'
